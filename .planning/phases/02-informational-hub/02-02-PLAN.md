# Plan 02-02: Academics & Course Catalog

**Wave:** 2
**Depends on:** 02-01
**Files modified:** `lib/data/courses.json`, `components/CourseAccordion.tsx`, `app/academics/page.tsx`, `app/academics/[slug]/page.tsx`
**Autonomous:** true

## Goal
Develop the academics overview and a detailed course catalog with department-wise grouping.

## Tasks

<task id="1">
<read_first>
- `lib/types.ts`
</read_first>
<action>
Create `lib/data/courses.json` populated with at least 8 courses across 3 departments (Science, Commerce, Arts).
Include fields for duration, eligibility, and a short description.
</action>
<acceptance_criteria>
- `lib/data/courses.json` contains valid JSON.
- At least 3 different `department` values present.
</acceptance_criteria>
</task>

<task id="2">
<read_first>
- `app/globals.css`
- `lib/types.ts`
</read_first>
<action>
Implement `components/CourseAccordion.tsx` using a clean disclosure pattern.
Show Department name as header, and list of courses inside.
Each course item should be a link to `app/academics/[slug]`.
</action>
<acceptance_criteria>
- Accordion is interactive and responsive.
- Uses `academic-navy` for headings and `academic-gold` for active indicators.
</acceptance_criteria>
</task>

<task id="3">
<read_first>
- `components/CourseAccordion.tsx`
- `lib/data/courses.json`
</read_first>
<action>
Create `app/academics/page.tsx`.
Display a hero section with "Our Programs".
Group the courses from JSON by department and render the accordions.
</action>
<acceptance_criteria>
- `/academics` page renders all courses grouped by department.
- Hero section follows UI-SPEC design.
</acceptance_criteria>
</task>

<task id="4">
<read_first>
- `lib/data/courses.json`
- `app/layout.tsx`
</read_first>
<action>
Create `app/academics/[slug]/page.tsx` (Dynamic Route).
Fetch individual course data by slug.
Display a detailed breakdown: Eligibility, Duration, Career Prospects.
Use a clean sidebar or grid layout for metadata.
</action>
<acceptance_criteria>
- Clicking a course in the catalog navigates to its detail page.
- 404 page shows if slug is invalid.
- Metadata is clearly presented in a "Fast Facts" section.
</acceptance_criteria>
</task>

## Verification
- Validate deep-linking for at least 2 different course slugs.
- Ensure "Learn More" links are accessible via keyboard.

## must_haves
- All 8+ courses are visible in the catalog.
- Course detail pages show eligibility and duration.
- Correct department-wise grouping.
