# Plan 02-03: About & Faculty Directory

**Wave:** 2
**Depends on:** 02-01
**Files modified:** `lib/data/faculty.json`, `components/FacultyCard.tsx`, `app/about/page.tsx`, `app/faculty/page.tsx`
**Autonomous:** true

## Goal
Implement the college history, mission, and vision pages, along with a professional faculty directory.

## Tasks

<task id="1">
<read_first>
- `lib/types.ts`
</read_first>
<action>
Create `lib/data/faculty.json` with 6+ staff profiles.
Include Name, Designation, Dept, Qualification, and a placeholder image path.
</action>
<acceptance_criteria>
- `lib/data/faculty.json` contains valid JSON.
- Mix of senior (HODs) and junior faculty included.
</acceptance_criteria>
</task>

<task id="2">
<read_first>
- `app/globals.css`
- `UI-SPEC.md`
</read_first>
<action>
Build `components/FacultyCard.tsx` following UI-SPEC (Grayscale to color hover, gold border).
Include small social/email icons at the bottom of the card.
Use `next/image` with `aspect-ratio: [4, 5]`.
</action>
<acceptance_criteria>
- Card displays Name, Designation, and Dept clearly.
- Grayscale filter applies to image by default, transitions to color on hover.
- Icons have `aria-label`.
</acceptance_criteria>
</task>

<task id="3">
<read_first>
- `app/layout.tsx`
- `app/globals.css`
</read_first>
<action>
Create `app/about/page.tsx`.
Sections:
1. Hero with "Excellence Since [Year]".
2. Mission & Vision (Glassmorphism cards side-by-side).
3. History (Vertical timeline or alternating text/image blocks).
Use `generate_image` for a stunning "Academic Milestone" image if needed.
</action>
<acceptance_criteria>
- Mission & Vision text is balanced (`text-wrap: balance`).
- Layout is responsive and visually "premium".
- History section uses brand colors for markers.
</acceptance_criteria>
</task>

<task id="4">
<read_first>
- `components/FacultyCard.tsx`
- `lib/data/faculty.json`
</read_first>
<action>
Create `app/faculty/page.tsx`.
Fetch faculty data and render in a responsive grid.
Add a filter sidebar (UI only for now) to show Departments.
Include a "Join Our Team" call-to-action at the bottom.
</action>
<acceptance_criteria>
- All 6+ faculty members are displayed in the grid.
- CTA is visually prominent with a Gold button.
</acceptance_criteria>
</task>

## Verification
- Check image optimization (Lighthouse/Performance) for the faculty grid.
- Verify all links in the about page timeline are valid.

## must_haves
- About page includes Mission and History.
- Faculty grid is responsive (2-4 columns).
- Images handle loading states without layout shifts.
