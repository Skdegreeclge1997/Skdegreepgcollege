# Plan 02-01: Data Models & Notice Board

**Wave:** 1
**Depends on:** Phase 1
**Files modified:** `lib/data/notices.json`, `components/NoticeCard.tsx`, `app/notices/page.tsx`, `lib/types.ts`
**Autonomous:** true

## Goal
Implement the data types and the dynamic notice board component with a premium grid layout.

## Tasks

<task id="1">
<read_first>
- `app/globals.css`
- `components/Navbar.tsx`
</read_first>
<action>
Create `lib/types.ts` defining `Notice`, `Course`, and `Faculty` interfaces based on the research schema.
Include `export type NoticeCategory = 'Admission' | 'Exam' | 'Event' | 'Holiday' | 'General';`.
</action>
<acceptance_criteria>
- `lib/types.ts` exists and exports the specified interfaces.
- Interfaces include all fields from RESEARCH.md.
</acceptance_criteria>
</task>

<task id="2">
<read_first>
- `lib/types.ts`
</read_first>
<action>
Create `lib/data/notices.json` with at least 5 sample notices (mix of categories, some pinned).
Ensure dates are in ISO format.
</action>
<acceptance_criteria>
- `lib/data/notices.json` contains valid JSON.
- At least one notice has `isPinned: true`.
</acceptance_criteria>
</task>

<task id="3">
<read_first>
- `lib/types.ts`
- `components/Footer.tsx` (for brand color reference)
</read_first>
<action>
Build `components/NoticeCard.tsx` following the UI-SPEC (Gold border, hover lift, category badges).
Use `lucide-react` icons (Calendar, Pin, Bell).
Add `aria-hidden="true"` to all icons.
</action>
<acceptance_criteria>
- `components/NoticeCard.tsx` uses the `.glass` utility or brand borders.
- Hover effects lift the card and increase border opacity.
- Badge colors correspond to categories.
</acceptance_criteria>
</task>

<task id="4">
<read_first>
- `app/layout.tsx`
- `components/NoticeCard.tsx`
</read_first>
<action>
Create `app/notices/page.tsx` as a Server Component.
Fetch notices from `lib/data/notices.json`.
Sort by `isPinned` (desc) then `date` (desc).
Render using a responsive grid with a "Recent Announcements" header.
</action>
<acceptance_criteria>
- `/notices` route is accessible.
- Notices are displayed in correct order (Pinned first).
- Header uses `text-wrap: balance`.
</acceptance_criteria>
</task>

## Verification
- Run `npm run build` to ensure type safety.
- Check accessibility of the grid via `gsd-ui-review` patterns.

## must_haves
- Notice board displays at least 5 announcements.
- Pinned notices are visually distinct (e.g., Gold pin icon).
- Responsive layout works on mobile (1 col) and desktop (3 col).
